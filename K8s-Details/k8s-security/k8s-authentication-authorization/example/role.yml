apiVersion: rbac.authorization.k8s.io/v1         #named group
kind: Role
metadata:
  namespace: default
  name: pod-reader
rules:
- apiGroups: [""]                 # "" indicates the core API group
  resources: ["pods"]
  verbs: ["get", "watch", "list"]    # read-only access to pods

