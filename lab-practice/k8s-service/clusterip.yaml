apiVersion: v1
kind: Service
metadata:
  name: clusterip-svc
  labels:
    app: clusterip
    env: dev
spec:
  type: ClusterIP
  ports:
      port: 80
      targetPort: 80
  selector:
      env: dev

