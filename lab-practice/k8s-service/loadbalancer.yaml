apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-svc
  labels:
    app: loadbalancer
    env: dev
spec:
  type: LoadBalancer
  ports:
      port: 80
      targetPort: 80
  selector:
      env: dev

